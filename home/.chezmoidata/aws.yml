# AWS Configuration for Dotfiles
aws:
  enabled: true # Single control for all AWS integration
  default_region: 'eu-west-3'
  default_output: 'json'

  # Bitwarden integration method
  use_bws: true # true = use BWS (recommended), false = use BW CLI
