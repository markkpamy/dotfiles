# Common aliases for both Bash and Fish
# Linux-specific aliases
alias ls="lsd"

# General aliases
alias cr='clear'
alias du="dust"
alias df="duf"
alias lzg='lazygit'
alias lzd='lazydocker'
alias zj='zellij'
alias z='zoxide'
alias cat='bat'
alias bc='bc -l'
alias h='history | nl'

# Docker compose aliases
# Start the docker-compose stack in the current directory
alias dcu="docker compose up -d"

# Start the docker-compose stack in the current directory and rebuild the images
alias dcub="docker compose up -d --build"

# Stop, delete (down) or restart the docker-compose stack in the current directory
alias dcs="docker compose stop"
alias dcd="docker compose down"
alias dcr="docker compose restart"

# Show the logs for the docker-compose stack in the current directory
alias dcl="docker compose logs"

# Quickly run the docker exec command
alias dex="docker exec -it"

# 'docker ps' displays the currently running containers
alias dps="docker ps"

# Stop all running containers
alias dsa="docker ps -q | xargs -r docker stop"

# Remove stopped containers, unused images, unused networks, etc.
alias dsp="docker system prune"

# FluxCD shortcuts
alias fxs='flux get sources all -A'
# Get all sources across namespaces
alias fxk='flux get kustomizations -A'
# Get all kustomizations across namespaces
alias fxh='flux get helmreleases -A'
# Get all helm releases across namespaces
alias fxr='flux reconcile'
# Shortcut for reconcile command
alias fxrk='flux reconcile kustomization'
# Reconcile kustomization
alias fxrh='flux reconcile helmrelease'
# Reconcile helmrelease
alias fxrs='flux reconcile source'
# Reconcile source
alias fxl='flux logs'
# View flux logs
alias fxe='flux events -A'
# Show all flux events
alias fxsusp='flux suspend'
# Suspend flux resource
alias fxres='flux resume'
# Resume flux resource

# More advanced aliases
alias fxrhf='flux reconcile helmrelease -n flux-system'
# Reconcile helmrelease in flux-system namespace
alias fxrkf='flux reconcile kustomization -n flux-system'
# Reconcile kustomization in flux-system namespace
alias fxhr='flux get helmreleases --status-selector ready=false -A'
# Get non-ready helm releases
alias fxkr='flux get kustomizations --status-selector ready=false -A'
# Get non-ready kustomizations
alias fxpend='flux get all -A | grep -i progressing'
# Show all progressing resources
alias fxfail='flux get all -A | grep -i failed'
# Show all failed resources

# Resource troubleshooting
alias fxtree='flux tree kustomization'
# Show kustomization tree
alias fxgtk='flux get kustomization'
# Get kustomization details
alias fxgth='flux get helmrelease'
# Get helmrelease details

# Terraform aliases
alias tf='terraform'
alias tfi='terraform init'
alias tfp='terraform plan'
alias tfa='terraform apply'
alias tfd='terraform destroy'
alias tfv='terraform validate'
alias tff='terraform fmt'
alias tfs='terraform show'
alias tfo='terraform output'
alias tfr='terraform refresh'
alias tfw='terraform workspace'
alias tfwl='terraform workspace list'
alias tfws='terraform workspace select'
alias tfwn='terraform workspace new'
alias tfc='terraform console'
alias tfg='terraform graph'
alias tft='terraform taint'
alias tfu='terraform untaint'
alias tfim='terraform import'
alias tfst='terraform state'
alias tfsl='terraform state list'
alias tfss='terraform state show'
alias tfrm='terraform state rm'
alias tfmv='terraform state mv'

# Terraform with common options
alias tfpl='terraform plan -out=tfplan'
# Plan and save to file
alias tfap='terraform apply tfplan'
# Apply from plan file
alias tfaa='terraform apply -auto-approve'
# Apply without confirmation
alias tfda='terraform destroy -auto-approve'
# Destroy without confirmation
alias tfpar='terraform plan -refresh-only'
# Plan refresh only
alias tfar='terraform apply -refresh-only'
# Apply refresh only

# OpenTofu aliases (drop-in Terraform replacement)
alias tofu='tofu'
alias tofui='tofu init'
alias tofup='tofu plan'
alias tofua='tofu apply'
alias tofud='tofu destroy'
alias tofuv='tofu validate'
alias tofuf='tofu fmt'
alias tofus='tofu show'
alias tofuo='tofu output'
alias tofur='tofu refresh'
alias tofuw='tofu workspace'
alias tofuwl='tofu workspace list'
alias tofuws='tofu workspace select'
alias tofuwn='tofu workspace new'
alias tofuc='tofu console'
alias tofug='tofu graph'
alias tofut='tofu taint'
alias tofuun='tofu untaint'

# Terragrunt aliases
alias tg='terragrunt'
alias tgi='terragrunt init'
alias tgp='terragrunt plan'
alias tga='terragrunt apply'
alias tgd='terragrunt destroy'
alias tgv='terragrunt validate'
alias tgf='terragrunt fmt'
alias tgo='terragrunt output'
alias tgaa='terragrunt apply-all'
alias tgda='terragrunt destroy-all'
alias tgpa='terragrunt plan-all'
alias tgra='terragrunt run-all'
alias tgiac='terragrunt init-all'

# Kubernetes aliases
alias k='kubectl'
alias ka='kubectl apply -f'
alias kf='kubectl create -f'
alias ke='kubectl explain'
alias kg='kubectl get'
alias kd='kubectl describe'
alias kk='kubectl krew'
alias kgaa='kubectl get all -A'

# Namespaces
alias kgns='kubectl get namespaces'

# Pod
alias kdp='kubectl describe pod'
alias kgp='kubectl get pods'
alias kgpa='kubectl get pod --all-namespaces'

# Service
alias kds='kubectl describe svc'
alias kgs='kubectl get svc'
alias kgsa='kubectl get svc --all-namespaces'

# Deployments
alias kdsd='kubectl describe deploy'
alias kgd='kubectl get deploy'
alias kgda='kubectl get deploy --all-namespaces'

# Nodes
alias kgn='kubectl get nodes'

# Ceph (Linux only)
alias ceph="kubectl rook-ceph ceph"

# System commands
# Linux system commands
alias reboot='sudo /sbin/reboot'
alias poweroff='sudo /sbin/poweroff'
alias halt='sudo /sbin/halt'
alias shutdown='sudo /sbin/shutdown'

# System info aliases
alias meminfo='free -m -l -t'
alias psmem='ps auxf | sort -nr -k 4'
alias psmem10='ps auxf | sort -nr -k 4 | head -10'
alias pscpu='ps auxf | sort -nr -k 3'
alias pscpu10='ps auxf | sort -nr -k 3 | head -10'
alias cpuinfo='lscpu'
alias gpumeminfo='grep -i --color memory /var/log/Xorg.0.log'

# Network utilities
alias myip='curl ifconfig.me'

# Include Bitwarden aliases if enabled
{{- if .bitwarden.enabled }}
{{ includeTemplate "bitwarden/aliases.tmpl" . }}
{{- end }}
